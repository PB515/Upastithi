# CLAUDE.md — Upasthiti

*The context anchor for this site. Read first every session. Keep Status and the Build Log current at the end of every phase.*

---

## What this is

A mobile-first PWA for Ramdootrestores (an NGO) replacing paper/Excel-based event attendance tracking, which currently fails because it depends on one specific, now-unreachable person. Three roles: **Management** (a different team member every event, mobile-only, no login — an event-scoped access code instead — marks existing attendees present or registers new walk-in volunteers, resilient to patchy field network), **Admin** (laptop, full control — creates events, generates/revokes access codes, downloads CSV per event, sees lightweight cross-event reporting for planning), **Viewer** (read-only, for other staff, added now to avoid a later rebuild). This is an **authenticated app with private per-role data** for Admin/Viewer, plus a genuinely different **anonymous, event-scoped access model for Management** — see decision 3 below, this is the one piece that deviates from the IDP's default portal pattern.

Full discovery process (forced questions, tier scorecard, all resolved decisions): [`docs/discovery-brief.md`](docs/discovery-brief.md).

## Current status

- **Phase:** 2 of 6 — Features — in progress
- **Last completed:** The Management access-token feature — the core goal of this whole app now works end to end. Admin generates a link + short fallback code on `/admin/events/[id]` (`generateGrant`/`revokeGrant`/`extendGrant` in that route's `actions.ts`, backed by `lib/management-token.ts`'s crypto per `data-model-security.md` §5 exactly — SHA-256 link token, `crypto.scrypt` salt:hash short code, zero new deps). `/e/[token]` verifies fresh on every request via `verifyManagementAccess()`, shows the event, lets Management mark/unmark attendance and register walk-ins — **no login**. Every write scoped to the server-verified `event_id`, never a client-supplied one. `Referrer-Policy: no-referrer` set on `/e/*`. `tooling/denial-gate.ts` extended with the cross-*event* items from §8 (grant isolation, expired/revoked denial, multi-grant-on-one-event isolation) — 22/22 checks now, closing the gap flagged since Phase 1. Browser-verified for real: generated a grant on "Diwali Volunteer Drive," opened the link cookie-cleared (no session), registered a walk-in ("Priya Shah"), toggled her present/absent both directions, revoked the grant, confirmed the same link then shows the generic "no longer active" message.
- **Mid-build fix worth noting**: caught that walk-in registration always marks present on creation, so a one-way "mark present" action would never have anything to act on — nobody could undo a mistaken entry. Changed `markPresent` to a real toggle (`setPresent(token, attendeeId, present)`) with an "Undo" affordance, so the capability decision 3 actually named ("mark present") is genuinely complete, not half-built.
- **Then Slice 3**: attendee list + CSV export on `/admin/events/[id]` (real table now, no more stub), an attendee-count badge on every event row (`events(count)` embedded query, no N+1), and text search + Upcoming/Past/All filtering on the event list — all client-side against the already-fetched list (`components/events-list.tsx`, now `'use client'`). All browser-verified: badge shows "1 attendee," search for "diwali" matches and "xyz" doesn't, Past tab correctly excludes the (future-dated) seeded event, Upcoming includes it, the attendee table shows Priya Shah's real row, and the CSV download was fetched and checked byte-for-byte (`Name,Phone,Present,Remarks,Registered At` header, her row, correct `Content-Disposition` filename).
- **Then Slice 4**: closed the two "decided but missing" gaps — Viewer-account management (`/admin/staff` — Admin adds a Viewer by email, gets a shown-once temp password, same "copy this now" pattern as grants; Remove deletes the `auth.users` row, cascades to `staff`) and the short-code entry page (`/e` — unified into the existing `/e/[token]` route rather than a separate rendering path: `verifyManagementAccess()` now tries the link-hash lookup first, falls through to a scrypt loop over active grants on a miss; rate-limit buckets split by credential shape, tight for code-shaped failures per §5.8). Extracted `requireAdmin()` out of `admin/events/[id]/actions.ts` into `lib/require-admin.ts` since Staff needed the identical check.
- **Two real bugs caught during this slice's own verification, both fixed, neither caught by rereading the docs**: (1) `events-list.tsx` went `'use client'` in Slice 3, which silently broke `/e/[token]/page.tsx` (a Server Component) importing `formatEventDate` from it — Next.js treats every export of a client-directive file as client-only, even a plain pure function, and this only fails at runtime, not build time. Extracted the formatter to `lib/format-date.ts`, a plain file neither side needs to think about. (2) **The audit trail has been silently non-functional since Slice 2.** Every `writeAuditLog()` call in `generateGrant`/`revokeGrant`/`extendGrant` (and now `addViewer`/`removeViewer`) uses the service-role client, but migration `0005` only granted `audit_log` INSERT to `authenticated` — it assumed audit writes would go through the admin's own session, which turned out wrong once those actions were actually built. Every entry failed silently (`writeAuditLog` logs and swallows by design) until the server logs were actually read during this slice's browser verification. Fixed with `0007_audit_log_service_role_grant.sql`; confirmed working by revoking a real grant and querying `audit_log` directly (`event_token.revoked` row present, correct label and timestamp).
- **Then Slice 5 — optimistic UI + offline retry queue for `/e/[token]`** (decision 4, confirmed as a real, high-stakes requirement — the field network "genuinely patchy, matters a lot," not a hedge). New `lib/patterns/offline-queue.ts` — a `useOfflineQueue` hook, `localStorage`-persisted, retrying on the `online` event / a 20s backstop / mount, distinguishing network failures (keep retrying) from application rejections like a revoked grant (fail immediately, no infinite retry) by how the error is shaped. `/e/[token]` moved from server-rendered forms to `management-client.tsx`, a Client Component with instant optimistic updates and a "Syncing…"/"Failed, tap to retry" state per row plus a summary banner.
- **Two more real bugs caught during this slice's own live testing, neither hypothetical**: (1) a reload-reconciliation bug — a queue item rehydrated from `localStorage` after a reload had no matching optimistic update in the local attendee list (only a fresh click ever got one), so once it finished syncing the UI kept showing the stale pre-sync value even though the database was already correct. Fixed by replacing the imperative per-click mutation with a single derived overlay (queue merged onto settled state at render time) that treats a fresh click and a rehydrated item identically. (2) A genuine duplicate write — registering a walk-in while offline, then restarting the server, produced **two** identical database rows. Cause: React Strict Mode double-invokes the mount effect in development, so the mount-time `flush()` ran twice before either invocation's `setQueue` had removed the item, and nothing stopped a second concurrent sync attempt on the same queue item. Fixed with a synchronous ref-backed in-flight `Set`, checked and updated before either React state or the network call. Confirmed fixed by literally repeating the same stop-server → register-offline → restart-server sequence and reading the database directly both times.
- One thing verified by code inspection rather than a full live repro: a write queued while genuinely offline, where the grant gets revoked *during* that offline window, correctly ending up `failed` not endlessly retried. Reproducing that exact timing live wasn't practical (the server-side page check blocks navigation to an already-revoked grant, so there's no way to get a mounted queue holding a pending item against it) — verified instead that the failure-classifier doesn't match a thrown `"This link is no longer active"` error as network-shaped, combined with the network-failure branch and the revoked-grant message both being independently proven live.
- Verified for real: generated a fresh grant, toggled attendance online (confirmed in DB), stopped the dev server entirely, toggled again — UI showed "Syncing…" and a "1 change waiting to sync" banner instantly, not a silent failure. Restarted the server, reloaded — the queued change had synced and the UI showed the correct settled state. Repeated with a walk-in registration while offline (appeared instantly with a temp id, confirmed absent from the DB while still offline) — synced correctly once back online, exactly one row. `npm run build` clean, `tooling/denial-gate.ts` 22/22 (untouched — no RLS/schema changes this slice), `npx vitest run` 25/25.
- **Then Slice 6 — `/admin/reports` (cross-event counts + trends), read-only for Viewer too.** No schema/RLS changes — pure application code on Phase 1's already-proven `events`/`attendees` read policies. New `components/reports-view.tsx` (pure display, no client state) takes `events`+`attendees` as props and computes per-event totals/present-counts by grouping in TypeScript rather than a fragile PostgREST filtered-aggregate query — same reasoning as Slice 3's client-side search. Thin pages at `/admin/reports` and `/viewer/reports` (matching decision 5/`app-prd.md`'s privacy matrix: Viewer gets Full on nothing else but read-only reports), "Reports" nav link added to both `/admin` and `/viewer` headers.
- **Not a bug, but worth logging:** browser-verification hit a rendering quirk specific to the automation tab, not the app — a long-lived dev-server session had accumulated stale Turbopack HMR console errors from bugs fixed slices ago (`formatEventDate`/`assertAdmin` references that no longer exist in source, confirmed by grepping the actual files), and separately the streamed RSC payload for a Suspense-boundary page would arrive in the network response but not visually swap into the tab's DOM until a hard reload. Confirmed real content both times by fetching the route directly and inspecting the response body/RSC payload rather than trusting the visible page, since the underlying HTML was always correct.
- Verified for real: signed in as `admin@upasthiti.test`, `/admin/reports` showed 1 event / 3 total attendees / 67% overall, with the Diwali Volunteer Drive row correctly showing 3 registered / 2 present / 67%. Signed in as `viewer@upasthiti.test`, `/viewer/reports` showed the identical numbers, read-only. Signed-out requests to either route redirect to `/login`; a Viewer session hitting `/admin/reports` redirects to `/login?error=Not authorized for this account`, same pattern as every other role-gated page. `npm run build` clean (new routes registered), `tooling/denial-gate.ts` 22/22 (untouched), `npx vitest run` 25/25.
- **Next up:** the deliberately-deferred items — regenerate (convenience wrapper over revoke+generate), PWA install wiring, design tokens/brand (still explicitly on hold per your instruction — finish functional build first).
- **Last commit:** `77a96c3` (Slices 1–4). Slices 5–6 not yet committed — ask before committing.
- **Resume note:** Local dev stack: `npm run doctor` → `npm run db:start` → `npm run migrate:up` → `npx tsx tooling/dev-seed.ts` (creates `admin@upasthiti.test` / `viewer@upasthiti.test`, both password `dev-<role>-password`, safe to rerun) → `npx tsx tooling/denial-gate.ts` to re-prove the gate anytime schema/RLS changes. **Gotcha hit this session, will recur**: local Supabase auth cookies aren't port-scoped in the browser — if another local IDP-cloned site's dev server was open in the same browser, its stale session cookie can shadow this site's on `localhost:<any port>` and make sign-in look broken. Clear cookies for the browser tab/origin if `/admin` or `/viewer` won't hold a session. Also: if two local Supabase projects are running at once they'll fight over ports 54321-54322 — `supabase stop --project-id <other>` first.

## Stack

Next.js App Router · Tailwind v4 + tokens · Supabase (4-client split + RLS, for Admin/Viewer only — Management uses a separate token-based access path, see decision 3) · Vercel. PWA scaffold already present in the template (`app/manifest.ts`, `public/sw.js`, `lib/pwa/register-sw.tsx`) — the Management side genuinely needs it (installable on the field team's phones).

## Conventions

Tokens only — no hardcoded hex · secrets in `.env.local` only · no new/upgraded deps without asking · git per phase, branch per phase · changing a frozen doc is a separate logged step. Full list: `docs/conventions.md`.

## Decisions made (do not revisit)

*From the discovery brief's "Resolved decisions" — nothing here was invented; everything traces to an explicit answer. See `docs/discovery-brief.md` for the full reasoning.*

1. **Craft tier = Essential, deliberately.** Discovery scorecard: 0/10, every hard override fires (task-critical flow, low-end/patchy-network audience, content-they-came-to-do). `reveal` + `smooth-scroll` only — no cinematic elements. This is a task-critical field tool; speed and reliability matter far more than polish. Capability tier is NOT small (full RBAC, event management, CRUD, CSV, reporting) — same pattern as the IDP's Inspire Academy precedent.
2. **Independent per-event attendee lists — no cross-event dedup by phone.** Matches the actual goal (per-event counts for planning), not individual attendance history. Simpler schema on purpose.
3. **★ Management access = event-scoped token, not a login.** Admin generates a random, unguessable, expiring (event date + 24h default, admin-extendable) grant per event — a link **and** a short fallback code together, not one or the other. Whoever has one can mark attendance / register volunteers for *that event only* — verified server-side, writes scoped narrowly (service-role-mediated), never broader. **A single event can have multiple simultaneous grants** — one per person actually working it, each independently labeled/revocable (the concrete case: a 150-person event needs 2+ people at once). "One grant per person" is an operational convention (issue a separate one each), not a technically enforced device lock — see `data-model-security.md` §5.9 for why, flag if that's wrong. **Known, accepted tradeoff:** no per-person audit trail on the Management side — only that an entry was marked via some grant on that event, not by whom or which grant. This deviates from the IDP's default portal pattern (`docs/golden-paths/portal.md` assumes every role is a logged-in Supabase Auth user) — Admin and Viewer still follow that pattern; Management does not. Full design in `docs/data-model-security.md` §5.
4. **Network: build for good connectivity, add a bounded fallback for zero signal.** Not full offline-first. Optimistic UI + a local retry/queue for writes that fail outright; clear pending/synced/failed state per entry. Per Safety Rail 8 (every runtime-dependent feature needs a planned failure path).
5. **Three roles, decided now to avoid a later rebuild:** Admin (laptop, full control), Management (mobile, token access, no login), Viewer (read-only, **confirmed real Supabase Auth login** — ongoing staff, not rotating per-event, so a one-time account is worth it; individually revocable, unlike a shared link). Viewer's read boundary beyond core event/attendee/report data is **confirmed deny by default** — no grant status, no audit log, no CSV export, same as any other role not explicitly granted something (`data-model-security.md` §4). Add a policy later if that turns out wrong; not a schema change.
6. **V1 data = name, phone, present, remarks (free text).** Admin-configurable structured per-event fields (e.g. a real dropdown for food preference) is explicitly **Phase 2**, agreed as out of scope for V1 — it's a small form-builder, meaningfully more engineering than the rest of this build.

## Where things live

- Discovery process + full reasoning → `docs/discovery-brief.md`
- Golden path (mostly applies — see decision 3 for where it doesn't) → `docs/golden-paths/portal.md`
- Tokens → `template/app/globals.css`
- Brand/contact constants → `template/lib/site.ts`
- Schema + migrations → `template/db/migrations/`
- PWA → `template/app/manifest.ts`, `template/public/sw.js`, `template/lib/pwa/register-sw.tsx`
- App PRD (roles, capabilities, privacy matrix, No-List) → `docs/app-prd.md`
- Data Model & Security (schema, RLS per table, the access-token design, the denial gate) → `docs/data-model-security.md`
- Auth pages → `template/app/login/`, `template/app/admin/page.tsx`, `template/app/viewer/page.tsx`
- Session middleware → `template/proxy.ts` (root convention file, Next 16 naming)
- Local dev bootstrap → `tooling/dev-seed.ts` (creates test admin/viewer accounts)
- Denial-gate proof → `tooling/denial-gate.ts` (DB-backed — lives in `tooling/`, not `template/tests/`, per `vitest.config.ts`'s own scoping: vitest there is pure `lib/logic/` unit tests only)
- Event CRUD → `template/app/admin/page.tsx` (list), `template/app/admin/events/new/` (create), `template/app/admin/events/[id]/` (view/edit), shared list rendering in `template/components/events-list.tsx`
- Management access-token crypto → `template/lib/management-token.ts` (generation, hashing, `verifyManagementAccess`)
- Admin grant management → `template/app/admin/events/[id]/actions.ts` (`generateGrant`/`revokeGrant`/`extendGrant`/`listGrants`) + `grants-panel.tsx`
- Management flow (no login) → `template/app/e/[token]/` (`page.tsx`, `actions.ts` — `setPresent`, `registerWalkIn`)
- CSV export → `template/app/admin/events/[id]/export/route.ts`
- Event list search/filter/count badge → `template/components/events-list.tsx`
- Cross-event reports (Admin full, Viewer read-only) → `template/components/reports-view.tsx`, `template/app/admin/reports/page.tsx`, `template/app/viewer/reports/page.tsx`
- Referrer-Policy header for `/e/*` → `template/next.config.ts`
- Shared admin auth check → `template/lib/require-admin.ts`
- Viewer-account management → `template/app/admin/staff/` (`actions.ts` — `listStaff`/`addViewer`/`removeViewer`, `staff-panel.tsx`)
- Short-code entry page → `template/app/e/page.tsx` + `actions.ts` (redirects into the unified `/e/[token]` verification)
- Shared date formatter → `template/lib/format-date.ts` (deliberately not in `events-list.tsx` — that file is `'use client'`, so a Server Component can't import from it)
- Offline retry queue → `template/lib/patterns/offline-queue.ts` (`useOfflineQueue` hook), consumed by `template/app/e/[token]/management-client.tsx`

## Known open items

None remaining from the original discovery-brief flags — all four are resolved: org name spelling confirmed, Viewer confirmed as a real login, Viewer's read boundary confirmed deny-by-default (no grant status/audit log/CSV export), and "one grant per person" confirmed as an operational convention (`data-model-security.md` §5.9). Nothing here is blocking Phase 1 start.

---

## Build log

*Newest last. One entry per phase: what was built, what was verified, any deviation. Before closing out a phase, also run `/cost` and log a row in the IDP's `docs/metrics/token-usage.md`.*

### Phase 0 — Setup
- Cloned from the Website IDP (`github.com/PB515/IDP`) into its own folder, git history to be started fresh.
- Ran the `discovery` skill end-to-end from a raw, verbal idea (no client-supplied docs) — forced questions, tier scorecard, 5 open questions asked and resolved, architecture note on the token-auth deviation. Full trail in `docs/discovery-brief.md`.
- Filled this file's Decisions section directly from the resolved discovery answers — nothing invented.
- Wrote `docs/app-prd.md` (roles/capabilities/privacy matrix/No-List) and `docs/data-model-security.md` (schema, RLS per table, the Management access-token design, the widened cross-role/cross-event denial gate) before any code, per the portal golden path's security-first order. Researched the sibling site's QR-token pattern for precedent (not part of the IDP template itself) and deliberately diverged from it — Management's token is a bearer/magic-link model, not a rotating-code-on-top-of-a-session model, since there's no session to layer on top of here.
- User feedback: wants both a link AND a short fallback code (not link-only), and multiple people need to work large events simultaneously (150-person case) — redesigned `event_access_tokens` from one-token-per-event to multiple labeled grants per event, each with its own link + code + independent revoke/extend. Confirmed default expiry: event date + 24h.
- Self-reviewed the resulting design and fixed several real gaps before freezing: a race-condition fix aimed at the wrong invariant once multi-grant became intentional (dropped, §5.7); a rate limiter that would've throttled legitimate shared-network usage (narrowed to failures only, §5.8); a hedged referrer-policy line (picked one); `audit_log.actor_id` left nullable for a "system actor" that didn't exist (made `not null`); a short-code verification design that assumed it could scope its lookup "to the event" and lock out "the closest candidate grant" on a wrong guess, which doesn't actually work once you notice Management has no login and so can't have picked an event before entering a code (§5.4, fixed to a global comparison + IP-based rate limiting); and finally caught that the short-code hash spec (`bcrypt`) would have required a new npm dependency, which needs asking first per Safety Rail 5 — swapped to Node's built-in `crypto.scrypt` (salt:hash, `timingSafeEqual` compare), zero new deps, consistent with the rest of the design already being Node-crypto-only.
- Closed the remaining 4 open items: org name confirmed correct; Viewer confirmed as a real Supabase Auth login (not a shared token, since staff use it repeatedly rather than rotating per-event); Viewer's read boundary confirmed deny-by-default beyond core event/attendee/report data (no grant status, roster, audit log, or CSV export); "one grant per person" confirmed as an operational convention, not device-binding. Both docs are frozen — no open design questions block starting the migration.
- Not yet done: `npm run setup`, `db:start`/`migrate:up`, the actual migration file, tokens (design tokens in `globals.css`), or any code.

### Phase 1 — Auth, RLS, cross-role denial
- Migrations `0003_roles.sql` (staff + `has_role()`), `0004_events_and_attendance.sql` (events/event_access_tokens/attendees/audit_log + RLS), `0005_table_grants.sql`, `0006_staff_service_role_grant.sql` — written from `data-model-security.md` §2/§3/§4 verbatim, applied clean, `db:check` shows no drift. Deleted the unapplied `0001_example.sql` per its own "safe to delete" header.
- Two real bugs the doc's SQL sketch had, both fixed while turning it into executable SQL: `for insert, update, delete` isn't valid Postgres policy syntax (FOR takes exactly one of ALL/SELECT/INSERT/UPDATE/DELETE) — consolidated to `FOR ALL` where the doc meant uniform admin access; and this migration runner connects as `postgres`, whose default privileges grant `anon`/`authenticated`/`service_role` only `TRUNCATE`/`REFERENCES`/`TRIGGER` on new tables, never `SELECT`/`INSERT`/`UPDATE`/`DELETE` — RLS policies alone were correctly deny-by-default, but nothing grants the base table access RLS then filters, so every table needed an explicit `GRANT` layer too (`0005`). Also missed granting `service_role` on `staff` at first, contradicting §2's own bootstrap flow (fixed in `0006`) — caught by actually running the bootstrap script, not by re-reading the doc.
- Built `/login` (email+password, no public signup — matches §2's "no self-serve become-admin path"), `template/proxy.ts` (session middleware — Next.js 16 renamed the `middleware.ts` convention to `proxy.ts`/`proxy` export, caught via a build warning and fixed), `/admin`, `/viewer` (bare pages, prove session+role routing only, no feature content).
- `tooling/dev-seed.ts` — local-only bootstrap for one admin + one viewer test account, idempotent (safe to rerun).
- `tooling/denial-gate.ts` — proves the cross-*role* items of §8 against a real local DB (anon fully denied everywhere, viewer read-only and confirmed denied on `event_access_tokens`/`audit_log`/writes, admin full CRUD except `event_access_tokens` which is zero-policy for everyone). 16/16 checks pass, teardown leaves the DB clean. Cross-*event* items (Management token scoping) are explicitly deferred — no `/e/[token]` route exists yet. Placed in `tooling/`, not `template/tests/`, matching `vitest.config.ts`'s existing scoping (pure `lib/logic/` unit tests only; DB-backed verification is `tooling/verify.ts`'s job) — also fixed that harness's own `selftest` to stop depending on `example_widget` (which `0001`'s deletion just removed) by making it create/drop its own throwaway probe table instead.
- Real local-dev infra hiccups hit and resolved along the way, logged since they'll recur: (1) Docker Desktop needed ~200s after launch before the daemon actually answered — `npm run doctor` correctly caught this. (2) A second local Supabase project (`garba_arts_admin`) was contending for ports 54321/54322 — stopped it (`supabase stop --project-id garba_arts_admin`, reversible, no data loss) to let this project's stack own the port cleanly. (3) That same conflict caused a stale cross-project session cookie to sit in the test browser (local Supabase's default cookie naming isn't port-scoped, so `localhost:<any port>` shares cookies) — cleared it manually; worth remembering if sign-in ever looks broken again with multiple local sites open.
- Verified: `npm run migrate:status` / `db:check` clean, `npx tsx tooling/denial-gate.ts` 16/16, `npm run build` (production) clean, `npx vitest run` 25/25 (unaffected), browser-verified admin/viewer sign-in, sign-out, and cross-role page denial end to end.
- Not yet done: any event/attendee CRUD UI, the `/e/[token]` Management flow, CSV export, reporting, Viewer-account management UI, PWA install wiring for the Management side, design tokens in `globals.css` (still placeholder magenta — fine for Phase 1's bare auth pages, needs doing before real feature UI per Safety Rail).

### Phase 2, Slice 1 — Admin/Viewer event CRUD
- Built event list (`/admin`, `/viewer` — shared `components/events-list.tsx`), create event (`/admin/events/new`), view/edit (`/admin/events/[id]`, using the existing `ViewEditForm` pattern rather than inventing a new view/edit state machine). No schema/RLS changes — this slice is pure application code on Phase 1's already-proven policies (`events` already had full admin CRUD + viewer-select).
- **Design tokens/brand deliberately deferred, on your explicit instruction** — you'll moodboard against the org's real logo and official color palette later; `globals.css` and `lib/site.ts` stay on the IDP's generic placeholders (magenta tokens, "Acme Co") for now. This is a real, logged deviation from the template's own "set tokens before any UI" convention, not an oversight — worth remembering to close out before this ships for real use, since a magenta admin panel isn't the intended final look.
- Scope deliberately held back from this slice (see plan): access-code/grant generation, attendee list + CSV export, `/admin/reports`, event deletion (never decided) — all separate rounds.
- Verified: `npm run build` clean, `npx tsx tooling/denial-gate.ts` still 16/16 (no regression from adding app code on top of proven RLS). Browser-verified end to end: created a real event as admin ("Diwali Volunteer Drive," 8 Nov 2026, Community Hall Ahmedabad), confirmed it renders correctly formatted in the list and on the detail page; signed in as viewer and confirmed the same event shows read-only with no create/edit affordances.
- Not yet done: everything held back above, plus the actual Management access-token feature this whole app's core value depends on — next up.

### Phase 2, Slice 2 — Management access grants + the `/e/[token]` flow
- The core value of this app now works: `template/lib/management-token.ts` implements `data-model-security.md` §5's crypto exactly (SHA-256 link token, `crypto.scrypt` salt:hash short code, Node built-ins only, zero new deps). Admin generates/revokes/extends grants on `/admin/events/[id]` (`generateGrant`/`revokeGrant`/`extendGrant`, service-role-mediated per §5.6, `assertAdmin()` re-checked in every action, not just relied on from the page render). `/e/[token]` re-verifies fresh on every load and every write (`verifyManagementAccess`), never trusts a prior check, scopes every write with `.eq('event_id', verifiedEventId)`. No schema changes — `event_access_tokens`/`attendees` already existed from Phase 1.
- Scope held deliberately tight, per the plan: short-code *entry page* deferred (hashing/storage still happens at generation, just no UI to redeem a bare code yet), regenerate deferred (revoke + generate covers it manually), optimistic UI + offline retry queue deferred (decision 4 — real, separate engineering), PWA install wiring deferred (ties to the branding deferral).
- Caught mid-verification, not by re-reading the doc: walk-in registration always marks present on creation, so a one-way `markPresent` action would have nothing to ever act on — no way to undo a mistaken entry. Fixed to a real toggle, `setPresent(token, attendeeId, present)`, with an "Undo" affordance — makes decision 3's actual named capability ("mark present") complete rather than half-built.
- Extended `tooling/denial-gate.ts` with the cross-*event* items from §8 that only became provable once `/e/[token]` existed: a grant resolves to its own event only; it cannot reach another event's attendee row (0 rows affected — the exact query shape the real Server Actions use, since `event_access_tokens` has zero RLS policies and this protection lives entirely in that `.eq('event_id', ...)` discipline, not a policy); expired and revoked tokens resolve to nothing; two grants on the same event are isolated (revoking one doesn't touch the other) — proving §5.2's multi-grant design holds. 22/22 checks now (16 cross-role + 6 cross-event), closing the gap flagged since Phase 1.
- Verified for real, not just automated: as admin, generated a labeled grant ("Gate volunteer") on "Diwali Volunteer Drive," confirmed the expiry math (event_date + 24h, UTC midnight → displayed correctly as 5:30am IST). Opened the link in a cookie-cleared browser context (no session) — event loaded with no login. Registered a walk-in ("Priya Shah"), confirmed she appeared marked present; toggled Undo/Mark present both directions and confirmed both persisted. Revoked the grant, reloaded the same link — got the generic "no longer active" message, not a crash. `Referrer-Policy: no-referrer` confirmed present on `/e/*` via a direct header check. `npm run build` clean, `npx vitest run` 25/25 (unaffected).
- Not yet done: the admin event detail page's "Attendees" section still shows the old empty-state stub even though real attendees now exist via `/e/[token]` — that's the very next thing (attendee list + CSV export), not a regression. Everything else held back above.

### Phase 2, Slice 3 — Attendee list, CSV export, count badge, search + filter
- `/admin/events/[id]` now shows a real attendee table (name, phone, present/absent, remarks) with a heading count, replacing the stub. "Export CSV" link next to it hits a new Route Handler (`template/app/admin/events/[id]/export/route.ts`) — auth-checked the same way as every other admin page (session → `has_role('admin')`), uses the admin's own session client since RLS already permits this read (no service-role needed, unlike the Management-token paths).
- Attendee count badge on every event row: `admin/page.tsx` and `viewer/page.tsx` both switched their query to `.select('*, attendees(count)')` (PostgREST embedded aggregate — one query, no N+1) and flatten the result into `attendeeCount` for `EventsList`.
- `components/events-list.tsx` converted to `'use client'` (nothing in it needed server-only APIs) and gained a text search (case-insensitive substring on name) and an All/Upcoming/Past date filter, both applied client-side against the already-fetched list — no new server round-trip, no pagination infrastructure. Deliberately not server-side search: fits this app's actual scale (an NGO's event history, not thousands of rows); flagged in the plan as a contained change to make later if that assumption ever breaks.
- Verified for real: badge shows "1 attendee" on the seeded event; searching "diwali" matches, "xyz-no-match" correctly shows "No events match your search"; Past tab excludes the (future-dated) event, Upcoming includes it. Attendee table shows Priya Shah's real row. CSV fetched directly and checked byte-for-byte — correct header row, her data, correct `Content-Disposition` filename (`diwali-volunteer-drive-attendees.csv`). `npm run build` clean, `tooling/denial-gate.ts` still 22/22 (no RLS/schema touched), `npx vitest run` 25/25 (unaffected).
- Not yet done: `/admin/reports` and everything else already listed as deferred (short-code entry page, regenerate, optimistic UI/offline queue, Viewer-account management, PWA install wiring, design tokens/brand).

### Phase 2, Slice 4 — Viewer-account management + the short-code entry page
- `/admin/staff`: Admin adds a Viewer by email (`addViewer` — service-role creates the `auth.users` row with a random temp password shown once, inserts the `staff` row, audit-logs), lists all staff, removes a Viewer (`removeViewer` — re-checks the target is actually role `viewer` before deleting, never usable to remove an Admin). Decision made without re-asking: temp password shown once, not an email invite — an invite needs real SMTP configured first, a separate decision; this needs nothing extra and matches how `tooling/dev-seed.ts` already works.
- Short-code entry (`/e`): unified into the existing `/e/[token]` route rather than building a second rendering path. `verifyManagementAccess()` in `lib/management-token.ts` now tries the link-hash lookup first (fast, indexed), and on a miss falls through to the §5.4 short-code path — a scrypt loop over every currently-active grant. `/e/page.tsx`'s form does no verification itself, it just redirects to `/e/<code>`, so the already-hardened route handles both credential shapes with the same "no oracle" denial message. Rate-limit buckets now split by credential shape (`looksLikeShortCode()`) — tight for code-shaped failures, generous for link-shaped, per §5.8.
- Extracted `requireAdmin()` (`lib/require-admin.ts`) out of `admin/events/[id]/actions.ts`'s local `assertAdmin()`, since `admin/staff/actions.ts` needed the identical check — one shared implementation instead of a second copy.
- **Two real, previously-undetected bugs caught during this slice's own verification** (both logged in Current Status above, repeated here since they're genuinely part of this slice's work, not incidental): the `formatEventDate` client/server boundary break from Slice 3's `'use client'` conversion (fixed: extracted to `lib/format-date.ts`), and the audit trail being silently non-functional since Slice 2 because `audit_log` was never granted to `service_role` (fixed: `0007_audit_log_service_role_grant.sql`, confirmed by revoking a real grant and reading the row back from the database directly, not just trusting the UI).
- Verified for real: added `newviewer@upasthiti.test` as admin, copied the shown-once temp password, signed in as that account cold and confirmed `/viewer` loaded; back as admin, removed the account and confirmed the same credentials then fail to sign in. Generated a grant, took its short code (not the link), redeemed it at `/e`, confirmed the same attendee-marking UI loaded and a walk-in registration through the code path persisted. Confirmed a Viewer session hitting `/admin/staff` redirects away with "Not authorized," same as any other admin-only page. `npm run build` clean, `tooling/denial-gate.ts` 22/22 (no regression — no RLS/schema changes this slice besides the audit_log grant fix), `npx vitest run` 25/25.
- Not yet done: `/admin/reports`, regenerate, optimistic UI + offline retry queue, PWA install wiring, design tokens/brand (still on hold, per your instruction).

### Phase 2, Slice 5 — Optimistic UI + offline retry queue for Management
- Decision 4's resilience layer, prioritized ahead of `/admin/reports` after you confirmed the field network is "genuinely patchy, matters a lot" — a real requirement, not a hedge. `template/lib/patterns/offline-queue.ts` (new pattern, alongside `view-edit-form.tsx` etc.): `useOfflineQueue`, `localStorage`-persisted, retries on the `online` event / a 20s interval backstop / mount. Network-shaped failures (fetch itself never reached the server) stay `pending` and keep retrying; application rejections (e.g. a revoked grant) become `failed` immediately — told apart automatically by error shape (`TypeError` / "fetch"/"network" in the message vs. anything else), no change needed to `setPresent`/`registerWalkIn` beyond `registerWalkIn` now returning `{ id }` instead of `void`, for reconciling a temp optimistic entry with its real id.
- `/e/[token]` moved from server-rendered `<form action>` to `management-client.tsx` — instant optimistic updates, a "Syncing…" badge (buttons disabled while in flight, deliberately — avoids ordering bugs like toggling a walk-in whose registration hasn't synced yet, simpler than a dependency graph between queued operations), a "Failed, tap to retry" affordance per row, and a summary banner when the queue is non-empty.
- **Two more real bugs, both caught only by actually stopping the dev server and reading the database, not by inspection**:
  1. *Reload reconciliation.* The first version optimistically mutated the local attendee list only inside the `toggle()`/`handleRegister()` click handlers — so a queue item rehydrated from `localStorage` after a page reload (no click event to hook into) had nothing keeping the UI in sync with it. Result: the database correctly showed the synced value, but the UI kept showing the stale pre-sync value even after the queued write finished. Fixed by replacing the imperative mutation with a single derived overlay — `displayAttendees` merges the queue onto the settled `attendees` state at render time, so a fresh click and a rehydrated reload are handled by the exact same code path instead of two.
  2. *Duplicate writes.* Registering a walk-in while offline, then restarting the server, created **two** identical rows, not one. Cause: React Strict Mode double-invokes the mount effect in development, so the mount-time `flush()` ran twice before either call's `setQueue` had removed the item from the pending list — nothing stopped a second concurrent sync attempt on the same queue item. Fixed with a synchronous ref-backed in-flight `Set`, checked before either React state (which batches) or the network call fires. Re-ran the identical stop-server → register-offline → restart-server sequence afterward and confirmed exactly one row both in the UI and via a direct database query.
- One thing verified by code inspection, not a full live repro, and logged as such rather than overclaiming: a write queued while genuinely offline where the grant is revoked *during* that window correctly lands `failed`. The exact timing isn't reproducible with the available tools — the server-side page check blocks navigation to an already-revoked grant before a client queue can even mount against it — so this rests on the failure-classifier correctly not matching a thrown `"This link is no longer active"` message as network-shaped, combined with the two halves that WERE independently proven live (the network-failure branch this session; the revoked-grant message back in Slice 2).
- Verified for real otherwise: toggled attendance online (confirmed via direct DB read), stopped the dev server entirely, toggled again — instant "Syncing…" plus a "1 change waiting to sync" banner, not a silent failure. Restarted the server, reloaded — correct synced state. Registered a walk-in while offline — appeared instantly with a temp id, confirmed genuinely absent from the database while offline, then synced to exactly one real row once reconnected. `npm run build` clean, `tooling/denial-gate.ts` 22/22 (untouched — no RLS/schema changes this slice), `npx vitest run` 25/25.
- Not yet done: `/admin/reports`, regenerate, PWA install wiring, design tokens/brand (still on hold, per your instruction).

### Phase 2, Slice 6 — `/admin/reports` and `/viewer/reports` (cross-event counts + trends)
- Read-only reporting across all events, decided per `app-prd.md`'s privacy matrix as one of the few things Viewer gets in addition to Admin (Full vs. Read-only, not Admin-only like everything else so far). No schema/RLS changes needed — both `events` and `attendees` already had the right read policies from Phase 1.
- `template/components/reports-view.tsx`: pure server-renderable display component, no client state. Takes `events`+`attendees` as props, groups attendees by `event_id` in a `Map` to compute per-event registered/present/attendance-rate, plus summary cards (total events, total attendees, overall rate). Deliberately not a PostgREST filtered-aggregate query (`.select('*, attendees(count).eq(present,true)')`-style) — same reasoning as Slice 3's client-side search: this app's real scale (an NGO's event history) makes TypeScript-side grouping simpler and just as correct.
- `template/app/admin/reports/page.tsx` and `template/app/viewer/reports/page.tsx`: thin Server Components, same auth-check pattern as every other role page, fetch via the user's own session client (RLS handles the read, no service-role needed). "Reports" nav link added to both `/admin` and `/viewer` headers.
- **Not an app bug, but real and worth logging**: verifying this in the browser hit two rendering quirks specific to the long-lived automation tab, not the app itself. (1) `read_console_messages` kept surfacing `formatEventDate`-multiply-defined and `assertAdmin is not defined` errors — both bugs from earlier slices, already fixed. Confirmed stale by grepping the actual current source (`lib/format-date.ts` is the sole definition, `lib/require-admin.ts`'s `requireAdmin()` is what's actually called) and by checking `preview_logs` showed zero live server errors — the console tool was surfacing an accumulated buffer, not fresh output. (2) Separately, real: the streamed RSC payload for a Suspense-boundary page (both reports pages use the shared `app/admin/loading.tsx` skeleton) would arrive correctly in the network response but not visually swap into the automation tab's DOM until a hard reload — confirmed the underlying HTML was always correct by fetching the route directly and inspecting the RSC payload/response body rather than trusting the rendered page.
- Verified for real once past that: signed in as `admin@upasthiti.test`, `/admin/reports` showed 1 event / 3 total attendees / 67% overall attendance, with the Diwali Volunteer Drive row correctly showing 3 registered / 2 present / 67%. Signed in as `viewer@upasthiti.test`, `/viewer/reports` showed the identical numbers, read-only (no create/edit affordances). Signed-out requests to either route redirect to `/login`. A Viewer session hitting `/admin/reports` redirects to `/login?error=Not authorized for this account`, same denial pattern as every other role-gated page. `npm run build` clean (both new routes registered), `tooling/denial-gate.ts` 22/22 (untouched, no RLS/schema changes), `npx vitest run` 25/25 (unaffected).
- Not yet done: regenerate (convenience wrapper over revoke+generate), PWA install wiring, design tokens/brand (still on hold, per your instruction).
